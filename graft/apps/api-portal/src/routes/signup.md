# apps/api-portal/src/routes/signup.tsx · [[api-portal-routing]]

Route module that renders the signup page requiring users to complete additional information before accessing the Global Fishing Watch REST API.

- Signup · function · L11-L32 — React component that renders the signup page, redirecting already-registered users to the home page unless they are explicitly editing their profile.
