# apps/api-portal/src/routes/index.tsx · [[api-portal-routing]]

Entry point module that defines the home route and renders the API portal dashboard with access token management interface.

- Home · function · L12-L28 — Renders the home page layout with descriptive text, access token list, and token creation form wrapped in a requirements check.
