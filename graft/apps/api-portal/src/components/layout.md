# apps/api-portal/src/components/layout.tsx · [[authenticated-layout-wrapper]] [[client-side-authentication-flow]]

Layout module that wraps the API portal interface with authentication checks, user header display, and conditional content rendering based on user permissions.

- Layout · function · L13-L56 — Root layout component that enforces user authentication and authorization, redirecting guests to login and displaying permission errors or authorized content accordingly.
