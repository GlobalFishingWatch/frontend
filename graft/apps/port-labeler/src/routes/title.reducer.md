# apps/port-labeler/src/routes/title.reducer.ts

Redux reducer module that manages the application title state, resetting it to the default 'GFW | Map' when navigating to the home route.

- titleReducer · function · L9-L16 — Redux reducer that updates the page title based on route navigation, returning the default title on HOME route and preserving the current state otherwise.
