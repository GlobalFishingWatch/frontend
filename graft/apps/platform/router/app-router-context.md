# apps/platform/router/app-router-context.ts · [[router-core]]

Provides type definitions and utilities for accessing the shared app store through router context.

- AppRouterContext · type · L3-L5 — Defines the shape of router context that optionally carries the application store.
- getAppRouterStore · function · L7-L9 — Extracts the application store from the provided router context using optional chaining.
