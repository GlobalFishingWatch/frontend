# apps/platform/queries/lazy-apis.types.ts · [[query-api-infrastructure]]

Type definitions file that extends the Redux store reducer module with lazy-loaded API slices from multiple domains.

- LazyLoadedSlices · interface · L13-L22 — Interface that aggregates all lazy-loaded Redux API slices (chat, data terminology, stats, events, insights, and search) into the root reducer store.
