# apps/platform/features/_vessels/vessel/identity/VesselIdentitySelector.tsx · [[time-range-filtering-pattern]] [[vessel-identity-selector]]

React component that renders a selector for switching between different vessel identities (self-reported or registry) within a specified time range.

- VesselIdentitySelector · function · L27-L97 — Renders an interactive list of vessel identities filtered by source, allowing users to switch identities and time periods with validation against the current timerange.
- setIdentityId · function · L42-L56 — Updates the selected vessel identity ID in query parameters and tracks the user action with analytics.
