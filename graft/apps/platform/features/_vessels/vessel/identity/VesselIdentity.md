# apps/platform/features/_vessels/vessel/identity/VesselIdentity.tsx · [[redux-state-selectors]] [[vessel-identity-display-system]]

React component that renders tabs for selecting and switching between different vessel identity sources, with analytics tracking.

- VesselIdentity · function · L14-L38 — React component that renders a tabbed interface for switching between vessel identity sources and tracks tab selection events.
- onTabClick · function · L19-L26 — Event handler that updates the query parameter for vessel identity source and tracks the tab click analytics event.
