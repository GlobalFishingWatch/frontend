# apps/platform/features/_map/dataviews/BasemapLabelsLocaleSync.tsx · [[basemap-locale-synchronization]]

Module that exports a React component to synchronize basemap labels locale with the current user interface language.

- BasemapLabelsLocaleSync · function · L11-L28 — React component that syncs the basemap dataview instance locale whenever the app language changes, updating the instance if the current locale differs from the stored one.
