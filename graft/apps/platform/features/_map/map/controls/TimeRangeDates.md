# apps/platform/features/_map/map/controls/TimeRangeDates.tsx · [[map-controls-system]]

Module that exports a React component for displaying a formatted date range with optional UTC label.

- TimeRangeDates · function · L7-L24 — React component that renders a localized date range display with automatic format selection based on the range span.
