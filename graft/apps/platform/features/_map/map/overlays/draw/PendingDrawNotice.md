# apps/platform/features/_map/map/overlays/draw/PendingDrawNotice.tsx · [[drawing-coordinate-system]]

Module exporting a React component that displays a loading notice popup during pending draw imports on the map.

- PendingDrawNotice · function · L12-L34 — React component that renders a centered popup with a loading spinner while a dataset import is in progress, positioned at the pending draw's geographic center.
