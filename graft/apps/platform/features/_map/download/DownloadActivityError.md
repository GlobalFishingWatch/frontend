# apps/platform/features/_map/download/DownloadActivityError.tsx · [[download-ui-form-components]]

React component that displays error messages for download activity operations, handling concurrent, timeout, and area-size validation errors.

- ActivityDownloadError · function · L16-L41 — React component that conditionally renders localized error messages for download failures based on error type (concurrent, timeout, area too large, or generic API error).
