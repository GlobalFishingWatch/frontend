# apps/platform/features/app/print.slice.ts · [[print-mode]]

Redux slice module that manages print mode state with toggle action and selector.

- LazyLoadedSlices · interface · L22-L22 — TypeScript module augmentation interface that registers the print slice as a lazy-loaded reducer.
