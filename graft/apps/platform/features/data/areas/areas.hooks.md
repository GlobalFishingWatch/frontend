# apps/platform/features/data/areas/areas.hooks.ts · [[data-layer-api]]

Custom React hook that fetches and returns dataset areas of context type for a given dataset ID.

- useFetchContextDatasetAreas · function · L8-L19 — Custom hook that dispatches a thunk to fetch context-type dataset areas by ID and returns the cached area list.
