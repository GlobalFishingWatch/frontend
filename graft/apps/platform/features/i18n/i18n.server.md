# apps/platform/features/i18n/i18n.server.ts · [[internationalization]]

- detectLanguageFromRequest · function · L29-L38 — Resolves the user's language preference from HTTP request headers (i18next cookie, query parameter, or Accept-Language).
- getLanguageResources · function · L45-L65 — Loads translation JSON files from disk for a language's namespaces with optional production caching to avoid repeated filesystem reads.
- PackageNsCacheEntry · type · L77-L77 — Data structure holding a cached translation namespace and its fetch timestamp for TTL-based cache expiry.
- fetchServerPackageNamespace · function · L80-L104 — Fetches CDN-hosted translation namespaces over the network with TTL-based caching and graceful fallback to stale data on network failure.
- loadServerPackageNamespaces · function · L111-L124 — Loads server-side package namespaces (e.g., country flags) into an i18next instance so SSR renders their translated values instead of fallback codes.
- invalidateServerPackageNamespaceCache · function · L131-L146 — Clears cached package namespace translations, optionally scoped by language or namespace, to pick up newly published translations without redeploy.
- createI18nForLanguage · function · L148-L170 — Instantiates and configures an i18next instance with loaded resources, React bindings, and fallback settings for a specified language.
- createRequestI18n · function · L172-L174 — Creates an i18next instance for a request using the language detected from its HTTP headers and parameters.
