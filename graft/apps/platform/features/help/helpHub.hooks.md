# apps/platform/features/help/helpHub.hooks.ts · [[scroll-driven-help-navigation]]

React hooks module that tracks active help items during scroll within a container.

- UseActiveItemOnScrollParams · type · L5-L9 — Configuration object that specifies a container reference, item slugs, and a callback for active item changes.
- useActiveItemOnScroll · function · L11-L52 — React hook that observes scroll position within a container and invokes a callback when a different item becomes visible in the active band.
