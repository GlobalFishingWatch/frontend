# apps/platform/features/_user/UserButton.tsx · [[login-ui-components]]

React component that displays a user profile button with authentication state handling, animation triggers on workspace/report loading completion, and conditional login/profile link rendering.

- UserButton · function · L25-L120 — React component that renders a user profile button with status animation, showing user initials when logged in or a login link when logged out or expired.
