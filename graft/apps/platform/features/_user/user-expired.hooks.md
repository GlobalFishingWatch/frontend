# apps/platform/features/_user/user-expired.hooks.tsx · [[user-authentication-session-management]]

Module exporting a React hook that displays and manages a toast notification when a user's session has expired.

- useUserExpiredToast · function · L12-L40 — Custom React hook that manages and displays a dismissible session-expired toast notification based on user expiration state.
- ToastContent · function · L16-L26 — Renders the localized session expiration message with a login link for user re-authentication.
