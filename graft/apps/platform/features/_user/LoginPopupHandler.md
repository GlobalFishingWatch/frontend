# apps/platform/features/_user/LoginPopupHandler.tsx · [[oauth-popup-login-token-handling]] [[user-authentication-session-management]]

Module that handles OAuth login flow in a popup window, extracting the access token from the URL and broadcasting the login event.

- LoginPopupHandler · function · L15-L42 — React component that manages the login popup flow by extracting and validating the access token, then closing or redirecting the window.
