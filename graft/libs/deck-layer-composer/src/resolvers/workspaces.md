# libs/deck-layer-composer/src/resolvers/workspaces.ts · [[type-specific-layer-resolvers]] [[user-data-and-polygon-layers]]

Module that exports a resolver function to transform workspace dataview configuration into deck layer properties.

- resolveDeckWorkspacesLayerProps · function · L5-L14 — Resolver function that extracts and maps workspace dataview properties (id, category, color, features) into the format required by the deck workspaces layer.
