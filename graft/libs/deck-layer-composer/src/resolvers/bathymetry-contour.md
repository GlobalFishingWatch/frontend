# libs/deck-layer-composer/src/resolvers/bathymetry-contour.ts · [[deck-layer-composition-rendering]]

Resolves bathymetry contour layer properties by extracting dataset configuration, applying depth filters (including exclude logic), and constructing the layer props for deck rendering.

- resolveDeckBathymetryContourLayerProps · function · L11-L42 — Resolves bathymetry contour layer properties by extracting dataset config, applying depth filters (including exclusion logic), and building the final layer props object.
