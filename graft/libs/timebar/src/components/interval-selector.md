# libs/timebar/src/components/interval-selector.tsx · [[interval-zoom-ui]]

React component that renders interval selection buttons allowing users to switch between different time granularities (year, month, day, etc.) and updates the timebar range accordingly.

- TimebarIntervalSelector · function · L15-L95 — React component that renders an interactive list of time interval buttons, calculating and applying new start/end date ranges when an interval is selected based on the latest available data or the midpoint of the current timebar.
