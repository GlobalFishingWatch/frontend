# libs/timebar/src/components/tool-wrapper.tsx · [[toolbar-tool-components]]

File exporting a wrapper component for toolbar buttons in the timebar UI.

- TimebarToolWrapper · function · L6-L25 — React component that wraps button elements with configurable click handlers and disabled states, applying conditional styling to reflect actionability.
