# libs/api-client/src/utils/guest.ts · [[guest-user-factory]] [[isomorphic-code-patterns-for-ssr]]

Module providing a static guest user object with anonymous permissions for synchronous resolution without network calls.

- getGuestUser · function · L7-L12 — Returns a static guest user with zero id, guest type, and anonymous permissions for synchronous client or SSR resolution.
