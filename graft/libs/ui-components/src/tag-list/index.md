# libs/ui-components/src/tag-list/index.ts · [[component-barrel-export-pattern]] [[tag-system]]

Entry point that exports the TagList component and defines tag-related types for managing a list of tagged items with optional tooltips and interactivity.

- TagItem · type · L6-L12 — Defines the structure of a single tag item with an id, label, and optional tooltip and interaction properties.
- TagListOnRemove · type · L18-L18 — Callback function type that notifies when a tag is removed, passing the removed tag and the updated tag list.
