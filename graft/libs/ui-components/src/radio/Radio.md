# libs/ui-components/src/radio/Radio.tsx · [[input-components]] [[input-validation-accessibility-pattern]]

Exports a reusable radio button UI component with label, tooltip, and disabled state support.

- RadioProps · interface · L12-L21 — Defines the configuration properties for the Radio component including active state, disabled state, label, click handler, and tooltip display options.
- Radio · function · L23-L62 — Renders an accessible radio button with an optional label and tooltip, delegating visual styling via CSS modules and managing click events through the onClick handler.
