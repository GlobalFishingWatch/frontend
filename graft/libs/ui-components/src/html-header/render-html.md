# libs/ui-components/src/html-header/render-html.tsx · [[header-navigation-system]]

Build script that pre-renders React Header components to static HTML files with embedded CSS styles.

- ComponentItem · type · L14-L17 — Data structure defining a React component and its target HTML output file path.
- preRender · function · L34-L48 — Renders a list of Header component variants to static HTML and writes each to disk with embedded CSS.
