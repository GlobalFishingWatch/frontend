# libs/ui-components/src/spinner/Spinner.tsx · [[css-modules-and-styling-architecture]] [[spinner-loading-indicator]]

Exports a reusable animated spinner React component that can be customized by size, color, and inline display mode.

- SpinnerProps · interface · L6-L11 — Defines the configuration interface for the Spinner component with optional color, size, className, and inline display properties.
- Spinner · function · L15-L41 — Renders an animated SVG spinner circle with configurable size, color, and layout mode, wrapping it in a centered container unless inline is true.
