# libs/ocean-areas/src/scripts/locales.ts · [[locale-generation-system]]

Script that extracts and sorts unique ocean area names from EEZ and FAO data sources, then writes them as source locale translations to a JSON file.

- start · function · L10-L27 — Generates a locale mapping of ocean area names by sorting combined EEZ and FAO features and writing the result as source translations.
