# libs/responsive-visualizations/src/types.d.ts

Type declarations for SVG and CSS module imports, enabling React components and style class name resolution in the responsive visualizations library.

- ClassNames · interface · L11-L13 — Interface that represents a flexible mapping of CSS class names to their string values.
