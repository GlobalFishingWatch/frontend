# libs/datasets-client/src/resolve-endpoint.ts · [[dataset-resolution-pipeline]]

Module that exports the resolveEndpoint function to convert dataset endpoint templates into concrete URLs.

- resolveEndpoint · function · L10-L106 — Generates a concrete URL by interpolating an endpoint template with dataset configuration parameters and query strings, applying dataset ID defaults when appropriate.
