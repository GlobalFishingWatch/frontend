# libs/deck-layers/src/config/sort.config.ts · [[layer-type-and-configuration-system]]

Configuration module that defines layer grouping and rendering order for map visualization layers.

- LayerGroup · enum · L4-L24 — Enum that categorizes map layers into functional groups (background, basemap, heatmaps, polygons, tracks, labels, overlays) to establish a layering hierarchy.
