# libs/deck-layers/src/layers/labels/labels.fonts.ts · [[asynchronous-font-loading]] [[labels-layer]]

Exports a deck font identifier and a function to asynchronously load the Roboto font from Google Fonts into the document.

- loadDeckFont · function · L3-L24 — Loads the DeckFont (Roboto) from Google Fonts and registers it with the document font face API, resolving a promise upon completion.
