# libs/dataviews-client/src/types.ts · [[type-system-contracts]]

- UrlDataviewInstance · type · L3-L6 — Represents a dataview instance that can be referenced from URL parameters with optional dataviewId and deletion flags for workspace configuration overrides.
- AnyDataviewInstance · type · L8-L8 — Union type that accepts either a standard DataviewInstance or a URL-bound UrlDataviewInstance.
