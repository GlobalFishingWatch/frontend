# libs/dataviews-client/esbuild.url.ts · [[bigquery-udf-bundle-export]]

Build script that compiles and bundles the URL workspace TypeScript library into a minified IIFE format for BigQuery UDF integration.

- buildBundle · function · L3-L64 — Async function that orchestrates esbuild to bundle the url-workspace library with BigQuery UDF compatibility settings including IIFE format, minification, and a require shim banner.
