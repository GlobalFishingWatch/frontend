import React, { Fragment, useContext, useMemo } from 'react'
import { TimebarChartData } from '.'

const TrackGraph = ({ data }: { data: TimebarChartData }) => {
  console.log(data)
  return <span>hello</span>
}

export default TrackGraph
