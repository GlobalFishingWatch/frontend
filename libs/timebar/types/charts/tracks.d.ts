export default Tracks;
declare function Tracks({ tracks }: {
    tracks: any;
}): JSX.Element;
declare namespace Tracks {
    namespace propTypes {
        const tracks: any;
    }
    namespace defaultProps {
        const tracks_1: any;
        export { tracks_1 as tracks };
    }
}
