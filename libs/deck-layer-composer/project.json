{
  "name": "deck-layer-composer",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/deck-layer-composer/src",
  "projectType": "library",
  "tags": ["lib"],
  "targets": {
    "dist": {
      "executor": "@nx/js:tsc"
    },
    "dev": {
      "dependsOn": ["^dev"],
      "executor": "@nx/js:tsc",
      "cache": false,
      "options": {
        "watch": true
      },
      "continuous": true
    },
    "publish": {
      "executor": "@nx/js:release-publish",
      "dependsOn": ["dist"],
      "options": {
        "packageRoot": "libs/deck-layer-composer"
      }
    },
    "test": {
      "executor": "@nx/vitest:test",
      "outputs": ["{options.reportsDirectory}"],
      "dependsOn": ["^dist"],
      "options": {
        "command": "vitest run --config=vitest.config.mts",
        "reportsDirectory": "../../coverage/libs/deck-layer-composer"
      }
    },
    "test:watch": {
      "executor": "@nx/vitest:test",
      "dependsOn": ["^dist"],
      "cache": false,
      "options": {
        "configFile": "libs/deck-layer-composer/vitest.config.mts",
        "watch": true
      },
      "continuous": true
    }
  }
}
