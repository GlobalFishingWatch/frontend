# libs/deck-layers

Non-obvious constraints on the deck.gl layers in this lib — both concern label rendering and
were each found the hard way by reading deck.gl's shaders. One fact per file in
`.claude/memory/`, same convention as the workspace root — see the root `CLAUDE.md`.

@.claude/memory/deck-collision-priority-range.md
@.claude/memory/label-layer-sdf-outline-opacity.md
