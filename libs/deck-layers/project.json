{
  "name": "deck-layers",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/deck-layers/src",
  "projectType": "library",
  "tags": ["lib"],
  "targets": {
    "dist": {
      "executor": "@nx/js:tsc",
      "options": {
        "clean": false
      }
    },
    "dev": {
      "dependsOn": ["^dev"],
      "executor": "@nx/js:tsc",
      "cache": false,
      "continuous": true,
      "options": {
        "watch": true
      }
    },
    "test": {
      "dependsOn": ["^dist"],
      "outputs": [
        "{projectRoot}/{options.coverage.reportsDirectory}",
        "{workspaceRoot}/coverage/libs/deck-layers"
      ],
      "options": {
        "command": "vitest run --config=vitest.config.mts",
        "coverage.reportsDirectory": "../../../coverage/libs/deck-layers"
      }
    },
    "test:watch": {
      "cache": false,
      "continuous": true,
      "options": {
        "watch": true,
        "config": "./vitest.config.mts"
      }
    },
    "publish": {
      "executor": "@nx/js:release-publish",
      "dependsOn": ["dist"],
      "options": {
        "packageRoot": "libs/deck-layers"
      }
    }
  }
}
