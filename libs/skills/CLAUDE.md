# libs/skills

Why this lib lives in this repo despite nothing in `apps/` importing it, and how to keep the
`encode-url` skill docs in sync. One fact per file in `.claude/memory/`, same convention as the
workspace root — see the root `CLAUDE.md`.

@.claude/memory/skills-lib.md
@.claude/memory/encode-url-skill-maintenance.md
