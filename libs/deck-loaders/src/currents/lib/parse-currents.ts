import type { LoaderContext } from '@loaders.gl/loader-utils'

import type { CurrentsLoaderOptions } from './types'

export const parseCurrents = (
  datasetsBuffer: ArrayBuffer,
  options: CurrentsLoaderOptions,
  context: LoaderContext
) => {
  debugger

  return datasetsBuffer
}
