{
  "name": "@platform/config",
  "version": "1.0.0",
  "private": true,
  "description": "platform app configuration constants, shared as source (no build) inside the monorepo",
  "type": "module",
  "exports": {
    "./routes": "./routes.ts",
    "./map/*": "./map/*.ts"
  },
  "dependencies": {
    "@globalfishingwatch/datasets-client": "workspace:^"
  }
}
