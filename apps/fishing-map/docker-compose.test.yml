services:
  test:
    image: mcr.microsoft.com/playwright:v1.58.2-noble
    volumes:
      - ../..:/workspace
    working_dir: /workspace
    environment:
      - VITE_PUBLIC_API_GATEWAY=https://gateway.api.dev.globalfishingwatch.org
      - VITE_PUBLIC_WORKSPACE_ENV=development
    command: bash -c "yarn install && yarn nx test fishing-map --browser='chromium'"
