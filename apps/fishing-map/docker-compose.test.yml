services:
  test:
    image: mcr.microsoft.com/playwright:v1.60.0-noble
    volumes:
      - ../..:/workspace
    working_dir: /workspace
    environment:
      - VITE_PUBLIC_API_GATEWAY=https://gateway.api.dev.globalfishingwatch.org
      - VITE_PUBLIC_WORKSPACE_ENV=development
    command: bash -c "pnpm install --frozen-lockfile && pnpm exec nx test fishing-map --browser='chromium'"
