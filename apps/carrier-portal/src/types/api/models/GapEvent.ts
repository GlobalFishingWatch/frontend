/**
 *
 * @export
 * @interface GapEvent
 */
export interface GapEvent {
  /**
   * Distance from shore when the event happened, in meters.
   * @type {number}
   * @memberof GapEvent
   */
  distanceFromShoreMeters: number
}
