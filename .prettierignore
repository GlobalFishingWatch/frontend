# Add files here to ignore them from prettier formatting

# Protobuf codegen — formatted only via proto:generate (explicit CLI paths bypass this ignore)
**/*.gen.js
**/*.gen.d.ts

# pnpm rewrites this unformatted on every install; formatting it is 16k lines of permanent churn
pnpm-lock.yaml

# Generated/large ocean-area datasets
libs/ocean-areas/src/data/**
libs/ocean-areas/src/source/**
libs/ui-components/src/miniglobe/ne_110m_land.json

/dist
/coverage

/.nx/cache
/.nx/workspace-data
.nx/self-healing
